<%= form_for(@food_vendor) do |f| %>
  <% if @food_vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_vendor.errors.count, "error") %> prohibited this food_vendor from being saved:</h2>

      <ul>
      <% @food_vendor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :food_id %><br />
    <%= f.number_field :food_id %>
  </div>
  <div class="field">
    <%= f.label :vendor_id %><br />
    <%= f.number_field :vendor_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
