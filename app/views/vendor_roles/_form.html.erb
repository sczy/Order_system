<%= form_for(@vendor_role) do |f| %>
  <% if @vendor_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vendor_role.errors.count, "error") %> prohibited this vendor_role from being saved:</h2>

      <ul>
      <% @vendor_role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :role %><br />
    <%= f.number_field :role ,:in => 0..1 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
