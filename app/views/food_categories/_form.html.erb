<%= form_for(@food_category) do |f| %>
  <% if @food_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_category.errors.count, "error") %> prohibited this food_category from being saved:</h2>

      <ul>
      <% @food_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :food_id %><br />
    <%= f.number_field :food_id %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.number_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
