<%= form_for(@vote_item) do |f| %>
  <% if @vote_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote_item.errors.count, "error") %> prohibited this vote_item from being saved:</h2>

      <ul>
      <% @vote_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :food_id %><br />
    <%= f.number_field :food_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
